# Neon Postgres connection string — auto-populated when you connect the
# database to this project from the Vercel dashboard (Storage tab).
DATABASE_URL=

# Basic-auth credentials for /admin/submissions.
ADMIN_USER=admin
ADMIN_PASSWORD=
